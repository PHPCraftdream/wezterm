# Checkpoint — 2026-08-08 21:37 [session-continuation]

## Session summary

This is a continuation of a very long multi-day OnlyTerm (Windows-focused wezterm fork) session. The visible window covered by this checkpoint: (1) diagnosing and fixing a real Hebrew bidi rendering bug that turned out NOT to be OnlyTerm's own terminal rendering (verified correct via a live plain-`echo` smoke test in a fresh cmd.exe window) but rather Claude Code's own ink-based TUI drawing Hebrew text without being bidi-aware; (2) building an auto-detect-and-disable-bidi-for-claude.exe feature (task #432) in response, including a real correctness bug I found and fixed myself (shaped-line render caches keyed only by content hash, not by the live foreground-process state, which would have served stale wrong-direction shapes across a foreground-process transition); (3) delegating a codebase-simplification pass to a `crush` sub-agent (session id `remove-nonwindows-ssh`), which completed Phase A (deleting ~15,262 LOC of macOS/X11/Wayland windowing backend code, commit `bf233a8a2`) and correctly determined Phase B (SSH/TLS removal) was already done in an earlier, not-visible-in-this-window part of the session (verified independently: `wezterm-ssh` genuinely doesn't exist anywhere in the tree); (4) zero-trust re-verifying crush's work myself, including using a throwaway `git worktree` to prove a `test::csi::test_789` failure predates crush's commit (pre-existing snapshot-staleness, unrelated to anything this session touched); (5) reading a fresh performance-audit document (`docs/investigations/2026-08-07-performance-audit.md`, already committed as `54c1b9fed` by some earlier point in the session) and decomposing its findings into 9 prioritized TaskList items (#433-#441).

Earlier in this same window (before the bidi work), a batch of UI/CLI features were built and fully verified (build/clippy -D warnings/fmt --check/test all clean, plus live smoke tests via a debug `onlyterm-gui.exe` and `onlyterm cli` for each): a `--start-conf <file>.ktav` flag that loads a multi-tab startup layout (global + per-tab `root_dir`/`vars`/`commands`, relative `root_dir` resolved against the layout file's own directory via `dunce::canonicalize` -- plain `Path::canonicalize` was tried first and found to produce a `\\?\`-prefixed path that `cmd.exe` rejects, a real bug caught by smoke testing, not just unit tests), mouse drag-to-reorder for tabs (reusing the existing `move_tab` used by `MoveTab`/`MoveTabRelative` key assignments), and a `RenameCurrentTab` action bound to `F2` by default plus double-click on the tab bar (also caught and fixed a real bug here: `derive.rs`'s `CommandDef.keys` field alone does NOT register a live default keybinding -- a separate `compute_default_actions()` list in `command_def.rs` is what actually wires it up, discovered when F2 fell through to `cmd.exe`'s own legacy line-editing "copy up to char" feature instead of triggering the rename prompt). Also created 4 `.ktav` startup-layout configs + 4 new `*-conf.vbs` launchers for the user's personal `dev_run` desktop folder (old `.vbs` scripts moved to a `legacy/` subfolder, new configs in a `configs/` subfolder) -- these `*-conf.vbs` launchers will only work once the user's INSTALLED release binary (`C:\Program Files\OnlyTerm\onlyterm-gui.exe`) is rebuilt; a user-initiated elevated `ci/dev-install.ps1` run failed on a transient `sccache` connection error (`os error 10054`), which I diagnosed and fixed by killing two stuck `sccache.exe` server processes that were racing on the same port -- the user has not yet retried the elevated install.

**None of this window's work is pushed.** Everything past `ce36bca34` (the last commit that was pushed, per an earlier checkpoint in this same long session) is local-only: `bf233a8a2` (crush's Phase A removal) and `54c1b9fed` (performance audit doc) are committed but unpushed; the start-conf/drag-n-drop/rename UI features and the bidi auto-disable feature are still UNCOMMITTED in the working tree (the user asked me to commit them, I was mid-planning a 2-commit split -- UI features, then bidi -- when the crush-agent-completion notification and then a performance-audit request interrupted that plan; the commit has not happened yet).

**Task #432 (bidi auto-disable) is code-complete but not confirmed working.** All wiring is done (config field, both `LineQuadCacheKey`/`LineToEleShapeCacheKey` cache keys carry the override bool, both `bidi_info()` call sites in `screen_line.rs` respect it), build/clippy/fmt/test all pass. A live smoke test was set up (a freshly-compiled test binary literally named `claude.exe`, confirmed via Win32 process listing to be the pane's real foreground process, printing Hebrew alongside an earlier plain-cmd.exe baseline Hebrew line in the same pane/window) and the user was asked to visually compare the two lines on screen -- **the user never answered that specific question** (conversation moved on to other requests). This is the single most important open thread: I do not know if the feature actually LOOKS right, only that it compiles/type-checks/passes automated tests, which cannot verify visual bidi correctness (confirmed earlier that `wezterm cli get-text` returns logical, not visual, cell order, so it's useless for this specific check).

No `/babysit` cron is currently armed (the earlier one, `eac6d1f1`, was explicitly deleted by the user along with 4 completed tasks a while before this window). No `/goal` Stop hook is active. The `crush-fallback` marker task was cleared to dormant and then explicitly deleted by the user this window (task #369, no longer exists).

## Active goal

None. No `/goal` Stop hook. No `/babysit` cron armed.

## TaskList

### in_progress
- #432 Auto-disable bidi reordering for claude.exe foreground process -- code complete, build/clippy/fmt/test all pass, live smoke test set up but user visual confirmation still outstanding (see session summary). Also carries a follow-up from the performance audit: hoist `bidi_disabled_by_foreground_process` to be computed once per pane per frame instead of once/twice per line.

### pending
- #408 СТОП-ПРАВИЛО: никогда не убивать чужие процессы OnlyTerm -- permanent standing marker, never meant to be completed.
- #433 P0 (performance audit): make `PaneOutput` delivery level-triggered/coalesced instead of a 3-hop queue (root cause of multi-second `spawn_delay` under sustained output).
- #434 P1 (performance audit): split WebGpu into a shared `ProcessGpuContext` + per-window `WindowGpuSurface`.
- #435 P1 (performance audit): switch quad rendering to instanced `QuadInstance` + persistent vertex buffer (272→84 bytes/quad).
- #436 P2 (performance audit): benchmark `Vec<BoxedQuad>`/chunked arena to replace Box-per-quad in `HeapQuadAllocator`.
- #437 P2 (performance audit): A/B benchmark custom `LfuCache` vs `lru::LruCache` for renderer caches.
- #438 P2 (performance audit): A/B benchmark `AHashMap` vs `std::HashMap` for hot internal render caches.
- #439 P2 (performance audit): add seqno-keyed immutable line-render-snapshot cache to avoid full `Line::clone` every render.
- #440 P3 (performance audit): investigate/fix duplicate `FontConfiguration`+`RenderMetrics` construction at GUI startup.
- #441 P3 (performance audit): benchmark `lto=thin`+`codegen-units=1` release profile (blockedBy: #433, #434, #435 -- must run last, after the algorithmic fixes, per the audit's own explicit sequencing).

### recently completed (this session, deleted from TaskList afterward per user request)
- #428/#429/#430/#431 -- `--start-conf` (+root_dir), tab drag-and-drop, tab rename (F2/double-click) -- all fully verified via live smoke tests, code still uncommitted (see session summary).
- #369 -- crush-fallback marker: cleared to dormant, then deleted entirely at user's explicit request this window.

## Decisions

- Chose to auto-detect the foreground process (`claude.exe`) and disable bidi per-pane, over a manual keybinding toggle or a global `bidi_enabled: false` -- fully automatic, no user action needed, and the detection mechanism (`Pane::get_foreground_process_name`, 300ms-TTL cached) already existed in the codebase for `skip_close_confirmation_for_processes_named`.
- Chose `dunce::canonicalize` over plain `std::fs::canonicalize` for resolving `--start-conf`'s relative `root_dir` -- plain canonicalize's `\\?\`-prefixed Windows verbatim path form is rejected by `cmd.exe` as a starting directory; caught by a live smoke test, not by unit tests, and added a dedicated regression test for the `\\?\` prefix specifically.
- Chose F2 (matching Windows Explorer's rename convention) + double-click on the tab bar as the rename triggers, explicitly WITHOUT a native context menu (user's explicit request) -- reuses the existing (previously dead-since-the-rhai-removal) `PromptInputLine` overlay machinery rather than building a new UI from scratch.
- Delegated the non-Windows-platform-code removal + SSH/TLS removal to a `crush` sub-agent as one session (`remove-nonwindows-ssh`) rather than splitting into two separate delegations; in hindsight this was fine since Phase B turned out to be a no-op (already done earlier), but the shared-file collision (crush's `git commit` swept up two of my own already-edited-but-uncommitted files, `Cargo.toml` and `command_def.rs`, into its Phase A commit) is a concrete lesson: a `crush` run's own commit step is not scoped to only the files it touched by design in this codebase, so running it in a working tree with substantial unrelated uncommitted changes has entangled-commit risk, not just parallel-run collision risk.
- Zero-trust-verified crush's report point-by-point rather than accepting it: independently re-ran build/clippy/fmt, and specifically proved the one test failure it reported as "pre-existing" really does predate its commit by using a disposable `git worktree` at the prior commit rather than trusting the self-report.

## Open questions

- **Does the bidi-auto-disable-for-claude.exe feature (#432) actually look correct on screen?** The user was shown a live test window with a `BASELINE`/`CLAUDE-LINE` Hebrew comparison and never answered. This needs to be re-asked (or re-demonstrated) before #432 can be marked complete.
- Should the UI features (start-conf/drag-n-drop/rename) and the bidi feature be committed as 2 separate commits as originally planned, or does the user want something different now that crush's commit already absorbed 2 of the shared files? (The 2-commit split is still achievable with the remaining uncommitted files; just needs to actually happen.)
- Has the user retried the elevated `ci/dev-install.ps1` run after the `sccache` server processes were killed? Unknown -- not yet confirmed either way.
- Should Phase B (SSH/TLS removal) task from the old `docs/plans/2026-07-23-remove-ssh-tls-feature.md` plan doc be formally closed out / the plan doc deleted, now that it's confirmed already done? Not yet decided or actioned.

## Repo state

```
 M Cargo.lock
 M crates/config/Cargo.toml
 M crates/config/src/config.rs
 M crates/config/src/keyassignment.rs
 M crates/config/src/lib.rs
 M crates/wezterm-gui-subcommands/src/lib.rs
 M crates/wezterm-gui/src/commands/derive.rs
 M crates/wezterm-gui/src/main.rs
 M crates/wezterm-gui/src/overlay/prompt.rs
 M crates/wezterm-gui/src/termwindow/actions.rs
 M crates/wezterm-gui/src/termwindow/mouseevent.rs
 M crates/wezterm-gui/src/termwindow/render/mod.rs
 M crates/wezterm-gui/src/termwindow/render/pane.rs
 M crates/wezterm-gui/src/termwindow/render/screen_line.rs
 M docs/changelog.md
 M docs/cli/start.md
 M docs/config/default-keys.md
 M docs/examples/cmd-synopsis-wezterm-start--help.txt
?? .claude/
?? ci/dev-install.log
?? crates/config/src/start_conf.rs
?? docs/config/reference/keyassignment/RenameCurrentTab.md
```

```
54c1b9fed docs: add performance audit
bf233a8a2 Phase A: Remove non-Windows platform backend code (macOS, X11, Wayland)
ce36bca34 window: fix double-free of the extra Rc ref on a create_window failure after WM_NCCREATE (task #402)
2acd837c1 docs: fix stale placeholder-clear comments found by @oh's review (task #411)
db47e078b docs: checkpoint closing out the #399-401/#405/#407 round (task #410)
```

Branch is 2 commits ahead of the last known push point (`ce36bca34`); neither `bf233a8a2` nor `54c1b9fed` has been pushed. The working tree has substantial uncommitted feature work on top, listed above.
