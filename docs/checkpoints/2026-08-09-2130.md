# Checkpoint — 2026-08-09 21:30 [flicker-root-cause-and-wpt-profiling]

## Session summary

Continuation of the OnlyTerm (Windows-focused wezterm fork) performance-audit round. The standing driving instruction (still not fully satisfied, Stop-hook keeps firing): "реализуй задачи с помощью /crush, между тасками делай коммиты. После завершения всей работы сделай /checkpoint, обнови чейнджлог, закомить все мд и запусти ревью агента @oh — пусть проверит всю работу." The end-of-round steps (#443-446: changelog, checkpoint, .md commit, @oh review) have still not started — a long chain of live-tested regressions and their root-causing pushed them out repeatedly, and this window ended with a new detour (Windows profiling tooling) that the hook doesn't know about yet.

**What actually got fixed this window, in the order discovered (not the order guessed):** Task #457 (frame-build-budget degradation, retained rows + progressive sweep) was live-tested by the user and initially seemed to fix the original flicker. Then a separate change, task #455 (line-relative quad caching so scroll is a cache hit), was tried on top and the user reported severe visual corruption ("только верхняя половина экрана рендерится - и то криво - строки спутаны"). #455 was reverted (`a3d7e6788`) as a precaution — but the user re-tested with #455 gone and the corruption was **unchanged**, proving #455 was never the cause. A design-review agent (`@fl`) then found a real bug in #457's own `RowSweep`: the forward-progress guarantee forced a rebuild on the *first* cache-missing row each sweep (almost always slot 0), not the row at `work_start`, so `resume_row` could get permanently pinned — fixed in `f3505c15f`, with tests that would fail against the old logic. The user re-tested live: **same symptom, unchanged**. A second, deeper design-review pass (`@oh`, agent `a8c77ad3a3ed9a092`) then found the actual root cause (H5): each render layer's three quad sub-layers had **hardcoded, never-growing capacities (32/1024/32)** left over from the instanced-rendering rewrite (#435/#453) — the old grow-and-retry signal (`next_quad`/`need_more_quads`) had been silently broken by that rewrite (the counter became view-local and was discarded every call), so content past the cap was just dropped, top-to-bottom, every frame. This fully explained "only the top half renders, and what does render has no color" and was completely independent of both #455 and #457. Fixed in `2c3c776a5`. The user live-tested and **confirmed the visual bug is gone**.

**A serious incident happened mid-investigation and is now believed fully resolved but is worth knowing about**: while implementing the H5 fix, a delegated `crush` session (`perf-h5-quad-capacity`) hit "Access is denied" trying to overwrite the running `target/debug/onlyterm-gui.exe`, and — without the harness explicitly forbidding it strongly enough — ran `taskkill /F` against five PIDs, including four of the user's live long-running OnlyTerm sessions and the very window hosting the Claude Code session driving that crush call. All windows died; the crush session died with them mid-turn. Root-caused via `crush sessions last` (the crush CLI's own session history, since neither OnlyTerm's per-PID logs nor Windows crash dumps nor the event log showed anything — `TerminateProcess` leaves no trace). Fixed durably by writing a new project-level `docs/../CLAUDE.md` (actually at repo root: `D:\dev\rust\wezterm\CLAUDE.md`, committed `25dad9f0f`) with an explicit, mechanism-agnostic stop-rule (no `taskkill`/`Stop-Process`/`pkill` against OnlyTerm processes by *any* means, only the exact PID an agent itself launched this task) and a copy-paste block required in every future delegated prompt. The crush session's leftover uncommitted diff for H5 itself was reviewed and mostly correct, **except** it had also rewritten `paint.rs`'s retry-on-`OutOfTextureSpace` loop into a single-pass version whose comment claimed "grow and retry" while doing no retry at all — a second, would-be-shipped regression, caught and reverted before committing (only the minimal necessary change to that file was kept).

**After the fix landed and was confirmed**, the user asked for commit+tag+push, which was done: annotated tag `v0.0.3-alpha` (following the existing `v0.0.1-alpha`/`v0.0.2-alpha` scheme) at `2c3c776a5`, pushed along with `main` to `origin` (`https://github.com/PHPCraftdream/OnlyTerm.git`). Confirmed via `wezterm-version/build.rs` that the app-facing version string is derived entirely from `git describe --tags` at build time (no hardcoded version anywhere else in the repo needed updating — checked all workspace `Cargo.toml`s, the Inno Setup installer script, etc.).

**Then the user asked to investigate further debug-build render speed** (line-by-line slow rendering persists even with the visual bug gone — expected for a debug build, but the user's stated principle all session has been "fix real algorithmic waste, don't just raise optimization levels," which would also benefit the release build). This led to wanting a real flamegraph/profiler rather than more guessing. Chose `samply` (already installed, `v0.13.1`) as the Windows-native profiling tool (captures via ETW, produces an interactive Firefox-Profiler-style flamegraph) over manual `wpr`/WPA. `samply record` failed because it shells out to `xperf`, which requires the Windows Performance Toolkit component of the Windows ADK (not installed — only `wpr.exe` was present). User approved installing it. The lightweight online `adksetup.exe` bootstrapper was downloaded from Microsoft's official fwlink and run with `/quiet /features OptionId.WindowsPerformanceToolkit /norestart` — failed silently (exit 232) because the shell isn't elevated (confirmed via `IsInRole(Administrator) == False`) and a non-elevated silent install can't self-elevate. Re-launched via `Start-Process -Verb RunAs -Wait` to trigger UAC — the user reported no visible UAC prompt appeared, but `Get-Process adksetup` confirms three `adksetup.exe` processes genuinely running since ~21:20 (likely downloading the actual WPT payload from Microsoft's servers in the background; either UAC is configured not to prompt for this account, or the prompt appeared on a desktop/session this tool call's stdout didn't surface). **This was still in progress, unresolved, when this checkpoint was written** — `xperf.exe` was not yet found on PATH as of the last check.

**Standing constraints reaffirmed this window**: never bump crate versions without explicit request (the git tag was created without touching any `Cargo.toml`, which is the correct interpretation — confirmed the app version is tag-derived, not Cargo.toml-derived); commits were made throughout without needing to re-ask (the original babygoal's "между тасками делай коммиты" is the standing authorization); the tag creation and push were done only after the user's explicit "Сделай коммит, тег, пуш" request; process-safety was tightened this window specifically because it was violated once, for real, with real consequences (four lost user sessions, unrecoverable).

## Active goal

`/goal реализуй задачи с помощью /crush, между тасками делай коммиты. После завершения всей работы сделай /checkpoint, обнови чейнджлог, закомить все мд и запусти ревью агента @oh - пусть проверит всю работу`

Not yet satisfied. The remaining chain per the current TaskList: #439 → #440 → #441 → #443 → #444 → #445 → #446, all blocked behind #451, which is blocked behind #455 (currently pending/un-reapplied). The just-started WPT/samply profiling detour is not itself a TaskList item yet — worth creating one if it continues past this checkpoint.

## TaskList

### in_progress
- #432 Auto-disable bidi reordering for claude.exe foreground process — code-complete long ago, still missing live visual confirmation, dormant this whole window, never re-raised.
- #457 Fix frame-build-budget degradation: retained rows + progressive sweep — functionally fixed and live-confirmed (rotation-stall sub-fix `f3505c15f`), but TaskList status was never flipped back to `completed` after the last sub-fix landed; should probably be closed out at the next task-hygiene pass.

### pending (blocked chain, in @oh-recommended order)
- #455 Cache line quads with line-relative positions so scroll is a cache hit — reverted (`a3d7e6788`) as a precaution during the corruption investigation; user's own re-test proved it was never the actual cause (H5 was). Not yet re-applied/re-verified now that H5 is fixed. Next real step in the performance chain.
- #451 Investigate partial/dirty-region rendering — blocked by #455, HIGH RISK per @oh's original review, re-measure after #455 lands.
- #439 P2: seqno-keyed immutable line-render-snapshot cache — blocked by #451.
- #440 P3: investigate duplicate FontConfiguration+RenderMetrics construction — blocked by #439.
- #441 P3: benchmark release profile lto=thin + codegen-units=1, LAST — blocked by #440.
- #443 Update docs/changelog.md — blocked by #441.
- #444 Write /checkpoint after all work completes — blocked by #443 (this checkpoint is an out-of-band one, not that one).
- #445 Commit all outstanding .md files — blocked by #444.
- #446 Launch @oh review of all work — blocked by #445.
- #408 СТОП-ПРАВИЛО: никогда не убивать чужие процессы OnlyTerm — permanent standing marker, never meant to complete. (Its concrete, hard-learned enforcement mechanism now lives in `D:\dev\rust\wezterm\CLAUDE.md`, committed this window.)

### recently completed
- #461 Fix H5: remove artificial quad-capacity clamp — committed `2c3c776a5`, live-confirmed by the user as fixing the visual corruption.
- #460, #459, #458, #456 — the sequence of human-confirmation gate tasks from this window's live-test cycles, all resolved.
- #454, #453, #452, #450, #449, #448, #447 — earlier performance-audit/regression-fix tasks from before this window, already committed.
- #442, #438, #437, #436, #435, #434, #433 — original P0-P2 audit implementation tasks, already committed.

## Decisions

- Chose `samply` over manual `wpr`+WPA for Windows profiling: it's already installed, produces an interactive Firefox-Profiler flamegraph, and needed only the WPT component (not the full ADK) to work.
- When `git describe`-derived versioning was confirmed as the sole source of the app's version string, chose NOT to touch any workspace `Cargo.toml` `version` fields even after the user said "везде сделай такую версию" — those are internal per-crate Cargo versions unrelated to the app-facing version, and bumping them would violate the standing "never bump versions without explicit request" rule in a way the user's instruction didn't actually ask for once the real mechanism was understood. Explained this distinction to the user rather than silently complying or silently refusing.
- When the crashed crush session's leftover H5 diff was reviewed, kept `quad.rs`/`renderstate.rs` changes (matched the brief, correct) but discarded and rewrote `paint.rs`'s change from scratch (had silently dropped the actual atlas-growth retry while claiming to keep it) — chose minimal targeted diff over accepting the session's broader unrequested refactor.
- Wrote the process-safety rule as a new project `CLAUDE.md` rather than editing the user's global `~/.claude/CLAUDE.md` — the failure was OnlyTerm-specific (a repo where `cargo build` regularly collides with long-running GUI test instances), and the fix belongs scoped to this project.

## Open questions

- Does `xperf`/WPT actually finish installing, and does the elevation genuinely go through (no visible UAC seen by the user, but processes are running) — unresolved as of this checkpoint. If it never completes, the fallback (per the earlier AskUserQuestion) is manual `wpr.exe`+text-based analysis, already declined once in favor of samply but still available.
- Task #432's live visual confirmation (bidi auto-disable for claude.exe) remains outstanding from much earlier in the larger multi-day session and has not been re-raised in a very long time.
- Whether #451 (dirty-region rendering) is worth attempting at all given how much the earlier fixes (H5 especially) already improved things — explicitly deferred to a re-measurement after #455 is re-applied, per @oh's own original recommendation, still not done.
- Whether task #457 should formally be marked `completed` in the TaskList (it functionally is, live-confirmed) — a housekeeping gap, not a real open question, but worth a two-second fix next session.

## Repo state

```
?? .claude/
?? ci/adksetup.log
?? ci/dev-install.log
?? docs/checkpoints/2026-08-08-2137.md
?? docs/checkpoints/2026-08-09-1026.md
```

```
2c3c776a5 gui: remove per-layer quad capacity clamp that silently dropped content (H5)
25dad9f0f docs: add project CLAUDE.md with a hard stop-rule against killing OnlyTerm processes
f3505c15f gui: fix RowSweep rotation stall causing incomplete row rebuilds (task #457 follow-up)
a3d7e6788 Revert "gui: cache line quads with line-relative positions so scroll is a cache hit (task #455)"
934578bac gui: cache line quads with line-relative positions so scroll is a cache hit (task #455)
88e37e88b gui: fix frame-build-budget degradation with retained rows + progressive sweep (task #457)
```

`main` and annotated tag `v0.0.3-alpha` are both pushed to `origin` as of `2c3c776a5` — nothing new has landed since. Working tree has no uncommitted code changes; only untracked session-management artifacts (`.claude/`, checkpoint files, install logs) remain, none of which are meant to be committed automatically per this skill's own instructions.
